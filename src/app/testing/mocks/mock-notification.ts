export default class MockNotificationService {
  messageSuccess(msg: string) {
    return msg;
  }

  messageError(msg: string) {
    return msg;
  }

  messageConfirm(msg: string) {
    return msg;
  }
}
